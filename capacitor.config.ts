import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionicproject',
  appName: 'ionic-project',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
